export type latestupdate = {
    chapterTranslationId: number
    chapterNumber: string
    
    uploadedAt: string
    viewCount: string
    languageId: string
    scanGroupName: string

    titleId: string
    titleName: string
    titleImg: string
}
