diff --git a/src/Components/Global/Titles/featuredtitles.scss b/src/Components/Global/Titles/featuredtitles.scss
index b86181b..2728d1a 100644
--- a/src/Components/Global/Titles/featuredtitles.scss
+++ b/src/Components/Global/Titles/featuredtitles.scss
@@ -81,7 +81,6 @@
         &__img {
             height: 100%;
             width: 20%;
-            border-radius: border-radius.$border-radius;
             @include no-select.no-select;
         }
 
diff --git a/src/Components/Global/Titles/featuredtitles.tsx b/src/Components/Global/Titles/featuredtitles.tsx
index a458471..7f40e97 100644
--- a/src/Components/Global/Titles/featuredtitles.tsx
+++ b/src/Components/Global/Titles/featuredtitles.tsx
@@ -56,7 +56,11 @@ export default function FeaturedTitles({ data }: { data: title[] }) {
                             <li className='featuredtitles__slide' key={`slide-${i}`}>
                                 <Link to={`/title/${title.id}/${title.name}`}>
                                     <article className='featuredtitles__card'>
-                                        <Img className={'featuredtitles__card__img'} src={`/manga-teste.jpg`} />
+                                        <Img
+                                            borderRadius={true}
+                                            className={'featuredtitles__card__img'}
+                                            src={`/manga-teste.jpg`}
+                                        />
                                         <section className='featuredtitles__card__section'>
                                             <Text not_exceed_X={true} className={`featuredtitles__card__name`} tag={'h3'}>
                                                 {title.name}
diff --git a/src/Components/Global/Titles/titlesgrid.scss b/src/Components/Global/Titles/titlesgrid.scss
index 4a59080..69dffbb 100644
--- a/src/Components/Global/Titles/titlesgrid.scss
+++ b/src/Components/Global/Titles/titlesgrid.scss
@@ -11,6 +11,7 @@
 
     gap: 1.5%;
 
+
     &__list {
         margin: 0;
         padding: 0;
@@ -30,7 +31,6 @@
         }
 
         &-img {
-            border-radius: border-radius.$border-radius;
             @include no-select.no-select;
         }
 
@@ -109,8 +109,8 @@
     }
 
     .titlegrid__item-img {
-        width: 30%;
-        height: 100%;
+        width: 80px;
+        height: 120px;
     }
 
     .titlegrid__item-name {
diff --git a/src/Components/Global/Titles/titlesgrid.tsx b/src/Components/Global/Titles/titlesgrid.tsx
index c771dd8..c4b8bad 100644
--- a/src/Components/Global/Titles/titlesgrid.tsx
+++ b/src/Components/Global/Titles/titlesgrid.tsx
@@ -97,10 +97,11 @@ export default function TitlesGrid({
                         <Img
                             className={'titlegrid__item-img'}
                             src={`/manga-teste.jpg`}
+                            borderRadius={true}
                             alt={`Cover of ${title.name}`}
                         />
                         {variant === 'compact' && (
-                            <TitleInfo title={title}/>
+                            <TitleInfo title={title} />
                         )}
                         {variant === 'card' && (
                             <TitleInfo title={title} config={{ contentRating: true, demographic: true, genres: true, themes: true, synopsis: true }} />
@@ -110,7 +111,7 @@ export default function TitlesGrid({
             </li>
         )
     }
-    
+
     return (
         <div
             className={`titlegrid titlegrid--${variant}`}
@@ -119,10 +120,10 @@ export default function TitlesGrid({
             }}
         >
             {
-                OrganizedData.map((subArray, s_index) => (
-                    <ul className={`titlegrid__list`} key={`titles-${s_index}`}>
+                Array.from({ length: columns }).map((_, column_i) => (
+                    <ul className="titlegrid__list" key={column_i}>
                         {
-                            subArray.map((title) => (
+                            OrganizedData[column_i]?.map((title) => (
                                 <TitleItem title={title} />
                             ))
                         }
diff --git a/src/Components/Global/img.tsx b/src/Components/Global/img.tsx
index 0fc8ea5..fda1ddf 100644
--- a/src/Components/Global/img.tsx
+++ b/src/Components/Global/img.tsx
@@ -1,25 +1,43 @@
+//# Libs //
+import { useState } from 'react'
 //# Utils //
 import FilterClasses from '../../Shared/utils/FilterClasses'
+//# Classes //
+import './img.scss'
 
 type Img = {
     /** src of the image */
     src: string
     /** additional CSS classes to apply */
     className?: string
+    /** disables the loading preview placeholder */
+    noPreview?: boolean
+    /** apply border-radius style */
+    borderRadius?: boolean
     /** alt of the image */
     alt?: string
     /** aria-hidden of the image */
     ariaHidden?: boolean
 }
 
-export default function Img({ src, className, alt, ariaHidden = undefined }: Img) {
+export default function Img({ src, className, alt, borderRadius, noPreview, ariaHidden = undefined }: Img) {
+    const [loaded, setLoaded] = useState(false)
     return (
-        <img
-            className={className ? FilterClasses(className) : undefined}
-            src={src || undefined}
-            alt={alt || undefined}
-            aria-hidden={ariaHidden}
-            loading='lazy'
-        />
+        <>
+            <div className={FilterClasses(`img__container ${className || ''}`)}>
+                {!noPreview && !loaded && (
+                    <div className={FilterClasses(`img__preview ${borderRadius ? 'img__borderRadius' : ''}`)} />
+                )}
+
+                <img
+                    className={FilterClasses(`img ${borderRadius ? 'img__borderRadius' : ''}`)}
+                    src={src || undefined}
+                    alt={alt || undefined}
+                    aria-hidden={ariaHidden}
+                    loading='lazy'
+                    onLoad={() => setLoaded(true)}
+                />
+            </div>
+        </>
     )
 }
\ No newline at end of file
diff --git a/src/Components/Title/title.scss b/src/Components/Title/title.scss
index 83fd48e..d7867eb 100644
--- a/src/Components/Title/title.scss
+++ b/src/Components/Title/title.scss
@@ -41,7 +41,6 @@ $text-shadow: 0px 0px 10px var(--text-color);
     }
 
     &__img {
-        border-radius: border-radius.$border-radius;
         width: 200px;
         height: 100%;
 
diff --git a/src/Components/Title/title.tsx b/src/Components/Title/title.tsx
index dfb2fb7..87f001f 100644
--- a/src/Components/Title/title.tsx
+++ b/src/Components/Title/title.tsx
@@ -163,9 +163,19 @@ export default function Title() {
                 Title: {data?.name}
             </Text >
             <div className='title__container'>
-                <Img className={'title__bg-img'} src={'/manga-teste.jpg'} ariaHidden={true} />
+                <Img
+                    className={'title__bg-img'}
+                    src={'/manga-teste.jpg'}
+                    ariaHidden={true}
+                    noPreview={true}
+                />
                 <header className='title__header'>
-                    <Img className={'title__img'} src={'/manga-teste.jpg'} alt={`Cover of ${data?.name}`} />
+                    <Img
+                        borderRadius={true}
+                        className={'title__img'}
+                        src={'/manga-teste.jpg'}
+                        alt={`Cover of ${data?.name}`}
+                    />
 
                     <section className={'title__header__content'}>
                         <section className='title__header__meta'>
